# 参考截面自动线性结构生成工具

> 日期：2026-07-26  
> 适用：NebulaeCraft / Minecraft Forge 1.12.2 / Java 8

## 物品与标记

- `nebulaecraft:autogen_marker`：跨栏式方向标记，橙色箭头表示方向。放置后可空手右键旋转；选择与碰撞范围按模型原始 `X 7..9、Y 0..13.5、Z 1.5..9` 及当前朝向旋转计算。
- `nebulaecraft:autogen_wand`：依次右键起点和终点标记以记录选区；潜行右键空气可清除选区。
- 两个箭头必须相向。标记所在方块是模板的中心路线基准；`tunnel_metro_1` 会在该格生成中央道床，`tunnel_metro_2` 会将该格作为双轨之间的空气。执行撤销时均恢复原标记。

## 指令

完整指令名为 `/nebulaetunnel`，可缩写为 `/ntunnel` 或 `/nautogen`，需要权限等级 2。

```text
/ntunnel preview <template> <template arguments>
/ntunnel confirm
/ntunnel cancel
/ntunnel status
/ntunnel undo
/ntunnel clear
```

当前内置模板为 `tunnel_metro_1` 与 `tunnel_metro_2`。
`tunnel_metro_1` 的参数格式为 `<platformColor> [mirror]`，固定生成单轨刚性接触网。

示例：

```text
/ntunnel preview tunnel_metro_1 cyan
/ntunnel preview tunnel_metro_1 yellow mirror
/ntunnel preview tunnel_metro_2
/ntunnel confirm
```

平台颜色支持 Railcraft 的 16 色英文名，额外接受 `silver`、`lightGray`、`lightBlue` 别名。
`tunnel_metro_2` 不接受额外参数，固定生成双轨、双刚性接触网与双侧墙灯。

## `tunnel_metro_1` 断面与材料

- 断面固定为 7×7；从上至下逐层为：
  - `7×强化混凝土`
  - `2×强化混凝土 + 强化混凝土上台阶(subtype1) + 钢性接触网 + 强化混凝土上台阶(subtype1) + 2×强化混凝土`
  - `强化混凝土 + 5×空气 + 强化混凝土`
  - `强化混凝土垂直台阶 + 5×空气 + 强化混凝土垂直台阶`（连续两层）
  - `强化混凝土 + 2×空气 + 强化轨道 + 2×空气 + 强化混凝土`
  - `2×强化混凝土 + 44:0 + 43:8 + 44:0 + 2×强化混凝土`
- 弯道相邻截面的空气区域取并集，避免衬砌在曲线内部形成横向立柱；平台和隧道灯作为设施占用净空格，但不再替换结构衬砌。
- 方格化曲线发生一格横向换位时，完整七层都使用旋转后的 3×8 专用过渡模板：`43:8` 中央道床和钢性接触网槽按 `1格 → 2格 → 1格` 斜向换位，`44:0` 与 subtype1 台阶分别在两侧补齐；顶板扩宽为 8 格，普通净空层扩为两侧外墙包住 6 格净空，两层垂直台阶同步移动到过渡外缘，避免相邻普通断面互相覆盖形成收窄、缺角和内部墙柱。
- 连续弯道由多组一格横移首尾拼接；90° 弯道中部的断面主轴从 X 切换到 Z 时，只保留一套不重叠的格化结构模板，被去重模板不再重复放置完整侧墙、地板或顶板。最终墙面仍由合并净空的真实外缘统一重建；垂直强化混凝土台阶的朝向不再按换轴轮廓长度均分，而是逐格采用最近中心曲线位置的解析切线。
- 上述去重只限制完整侧墙、地板和顶板模板；被去重的新轴候选仍把实际拥有的 `43:8/44:0`、对应净空缓冲及接触网凹槽语义并入最终结构，避免新轴方向的道床被旧轴墙体覆盖。
- 地板、侧墙、接触网凹槽的默认混凝土层与最外层顶板统一从最终合并净空推导，不再让后两层只依赖被保留的完整断面模板。每个最终净空格都会取得默认凹槽外层和顶板，显式的凹槽空气、水平台阶、刚性接触网及坡道抬高层再按既有优先级覆盖；因此换轴处即使去重一套重叠的 3×8 实体模板，其道床扩展出来的真实净空仍一定具有封闭顶部。
- 普通外轮廓格在中间两层始终使用垂直强化混凝土台阶。生成器保留每个方格化中心轨道格对应的五次 Hermite 曲线参数，并直接计算该点的解析切线：当 `|dx/dt| >= |dz/dt|`（切线更接近 Minecraft X 轴）时，垂直台阶使用 `facing=north/south`；当 `|dz/dt| > |dx/dt|`（切线更接近 North/South 轴）时使用 `facing=east/west`。朝向的正负方向选择指向中心线路的一侧。该规则会在最终结构完成后统一应用于全部垂直台阶，包括阶梯内角、换轴中心和模板残留格，不再受方格锯齿的瞬时方向或旧轴迟滞阈值影响。
- 同一解析切线也是隧道内其他旋转方块的统一方向来源。隧道灯的 `facing` 表示线路侧向，因此使用与垂直台阶相同的法线轴；直线、支撑和坡线刚性接触网的模型轴则与切线轴平行。坡线仍结合真实高低端选择正反方向。Railcraft 弯轨和刚性接触网斜线必须连接方格路线中两个实际相邻方向，因而保留连接拓扑决定的弯角/斜线象限；解析切线负责它们所属断面及进入/离开斜线前后的直线朝向，不能用单一卡迪纳尔轴替代实际连接。
- 墙后接缝按最终净空和最终正交墙边界推导，不再依赖某套 3×8 模板的固定候选坐标：一个外角只有在连接它与某个对角净空格的两个正交中间格都是真实墙格时，才补完整强化混凝土背衬。该条件能覆盖连续换位及 X/Z 换轴新增的缝隙，同时排除仅仅对角邻接净空的内侧格，不会重新生成侵入隧道的混凝土立柱。
- 整块衬砌：`railcraft:reinforced_concrete` meta 8（light gray）。
- 接触网槽两侧固定使用 NebulaeCraft 水平强化混凝土台阶 `subtype1`；中部两层侧墙固定使用朝外贴墙的垂直强化混凝土台阶。
- 强化轨道处于一格高差的下端、使用坡道模型时，该格改用 7×8 坡道专用断面；从上至下为：
  - `7×强化混凝土`
  - `2×强化混凝土 + 强化混凝土上台阶(subtype1) + 空气 + 强化混凝土上台阶(subtype1) + 2×强化混凝土`
  - `强化混凝土 + 2×空气 + 钢性接触网斜坡 + 2×空气 + 强化混凝土`
  - `强化混凝土垂直台阶 + 5×空气 + 强化混凝土垂直台阶`（连续三层）
  - `强化混凝土 + 2×空气 + 强化轨道斜坡 + 2×空气 + 强化混凝土`
  - `2×强化混凝土 + 3×43:8 + 2×强化混凝土`
  上端的平轨格仍使用普通断面，并与坡道格抬高一层的顶板自然衔接。
- 统一外轮廓在坡道交界会把同一 XZ 的相邻高度净空视为连续，并在写墙前逐格避让已有净空体积，因此低端断面不会在高端道床前形成横断墙。坡道原模板位于 `clearHeight` 的第三排垂直台阶会在外轮廓重建时保留，并按最终净空方向校正朝向；普通平坡墙仍只有两排。墙列只有在解析断面属于坡轨且墙脚本身也处于该坡轨的低层时才使用抬高顶板；若高侧墙的 X/Z 投影更接近低侧坡轨，它仍按自身高层的普通断面封顶，墙后外角背衬也不会被连带抬高。
- 本模组旋转方块不使用原版水平 metadata 顺序。生成器按实际模型显式使用 `N=0、S=1、W=2、E=3`，并分别处理侧贴模型及钢性接触网的斜线/坡线方向。
- 中央道床：旧 ID `43:8`，即 `minecraft:double_stone_slab` meta 8。
- 左右道床：旧 ID `44:0`，即 `minecraft:stone_slab` meta 0。
- 轨道：`railcraft:track_flex_reinforced`；方块解析仍兼容旧 ID `railcraft:reinforced_track` 和 `railcraft:track_reinforced`。
- Railcraft 弹性轨道首次放入世界时会按当时已经存在的邻轨自动改形。生成器会先完成整条轨道放置，再按路线计算结果执行一次不重复记录撤销数据的定向复写，保证连续弯轨及 90° 中段不会因分批生成顺序而断开。
- 弯道路基先于平台和墙面按整条轨道路网规划：每个轨道格固定使用 `43:8`，每段前后连接方向的两侧邻格组成 `44:0` 带；真正的坡轨格两侧仍使用 `43:8`，3×8 过渡模板额外扩宽的 `43:8/44:0` 也进入同一占位集合。若同一条 `44:0` 侧带在格化斜线处仅以角相接，生成器会在两个正交肘点中排除属于 `43:8` 中央道床的一个，并补入唯一的外侧肘点，直至每条侧带都以共享边四向连通；它不会强制两侧数量相等，例如 200 格的半径 100 弧线应保持外侧 202 格、内侧 198 格。全部道床格随后并入净空再提取墙面，因此垂直台阶墙列不会与 `43:8/44:0` 共用坐标。
- 全部 3×8 过渡候选（包括换轴时被去重的候选）都会参与道床占位；最终占位还向四个水平方向扩张一格作为混凝土地板与净空缓冲，路线开放端之外不扩张。因此垂直台阶墙列既不会与 `43:8/44:0` 共用坐标，也不会直接贴住侧道床。
- 道床净空缓冲在一格坡道交界处不会跨入同一 XZ 柱中高一层或低一层的相邻道床；因此高端道床的缓冲不会在低端三格 `43:8` 上方生成强化混凝土，低端缓冲也不会在高端道床下方留下隧道外的三格强化混凝土。同层曲线及 3×8 换位仍保留完整的四向缓冲。
- 平台默认位于起点至终点方向的左侧；`mirror` 会将平台及同侧隧道灯换到右侧。方格化曲线发生一格横向换位时，先计算每个过渡的新平台锚点；X/Z 轴切中被去重的结构模板仍保留自己的平台锚点。删除道床上方的无效旧格后，只有尚未被现有平台 8 邻接连通的相邻锚点才补斜线桥，避免在本来正常的过渡旁再铺一条平台。最终单格宽平台线在墙面生成前并入净空并取得完整强化混凝土基层，所有落在最终 `43:8/44:0` 上方的旧平台格统一删除，避免断线、错位和压占侧道床。
- 平台补桥不再使用唯一的整数取整斜线，而是在路线顺序相邻的过渡锚点局部包围盒内执行 8 邻接最短路。桥格必须已经位于最终净空、距线路 2–3 格、不属于道床、不越过开放端，也不能覆盖轨道等设施；因此可在两种等长斜线相位中选择不压 `43:8/44:0` 的一条，而不会跨接远处弯道。
- 跨层弯道的平台过渡锚点不直接继承中心路线格的 Y；待删除锚点和新增锚点都以自身 X/Z 经 `GradeLayout.floorAt` 取得解析坡面楼层，再上移一格。因此平台侧格跨过高低分界时，其高度与最终净空、道床和普通平台线使用同一分层规则。
- 隧道灯直接以 `lightSpacing` 作为路线索引周期；默认值为 9，放置索引固定为 `0, 9, 18...`，因此两盏灯之间有 8 格。灯具全部位于金属平台一侧；使用 `mirror` 时与平台同步换边。
- 每盏灯都从轨道中心沿平台侧扫描最终合并净空，放在首个非净空墙格之前的最后一格；连续斜线与 X/Z 换轴处即使外墙相对普通断面移动一格或两格，灯具仍会贴住实际外墙。扫描每前进一步都会按解析切线断面重新取得该横向格的楼层，跨越曲线坡道的高低分界时不会因只检查中心轨道 Y 而提前停在净空中。
- 灯具最终落点属于坡道断面时向上移动一格，以贴合坡道专用断面抬高后的完整侧墙；判断依据是灯具所在的墙前断面，而非同一索引的中心轨道格。
- 接触网支撑件同样直接以 `catenarySupportSpacing` 作为路线索引周期；默认值为 9，目标索引为 `0, 9, 18...`。若目标格是斜线或坡线接触网，则保留该特化模型，并在前后两格内选择最近的直线接触网格放置支撑件；若四个邻近候选均非直线，本次支撑件放置直接跳过。
- 坡线接触网只生成在真正使用上坡轨道状态的低端切面；与其相邻的高端平轨切面使用普通直线接触网。
- 曲线上的一格高差不会占用方格化横移边，坡轨低端也必须是能连接前后轨道的直轨格。为避免连续弯道把过长路段全部排除，3×8 过渡现允许在已完成横移后的低侧外边接入坡道：入段、中间横移和出段三行模板分别在自己的高低层生成，坡道专用断面再从低端衔接。正反向使用对称的低侧规则，不会把 Railcraft 上坡轨道与水平弯轨合并。
- 全部坡步现在一次性与可用直线边做全局匹配，而不再逐个贪心移动。相邻坡步的路线索引间距不小于 `ceil(1 / maximumGrade)`；默认最大坡度 10% 时至少间隔 10 格。这避免某个理论坡步跨过连续曲线模板后，与下一坡步挤在同一小段内。
- 隧道各横向格的楼层不再直接继承方格中心点的 Y，而是由每个坡道低端处的解析切线断面划分：以单位切线的点积判定格子位于高侧、低侧或坡道断面内。高低层分界因此是当前曲线切线的垂线，并以 Minecraft 方格阶梯逼近，不会在弯道中退化为固定 X/Z 行。
- 坡道专用七格断面沿上述解析法线逐格取整，取整前以法线两个分量中的较大绝对值缩放到 1，保证从 `-3..3` 的每个横向序号都对应唯一方块。因此接近 45° 的坡道不会因单位法线取整重复而把七格断面压缩成五格。曲线坡道两侧墙柱可位于不同的线路纵向坐标，但都使用三格高垂直强化混凝土台阶；接触网凹槽和最外顶板沿同一法线整体抬高一层。直线坡道是该规则的法线恰好平行 X/Z 轴时的特例，既有断面保持不变。
- 坡道解析法线中的五格净空是 `GradeLayout` 的权威低层断面，平台等设施与净空、道床使用同一归层结果；中央三格 `43:8` 也会在统一外壳生成前直接并入最终语义。同一 XZ 若被相邻高端断面误分配了高一层的设施、道床或净空，则删除高层占位并以坡道低端为准。坡道重放只负责五格净空内的接触网槽，不再强制覆盖法线两端；实际正交墙边界决定垂直台阶，墙后的对角格保持整块强化混凝土背衬。
- 若方格取整曾令上述五格中的边缘格落入相邻高层，坡道墙的第三排垂直台阶会在真实正交墙边界中比较法线端点及其高侧邻格，选择最接近坡步中点的一列；被移走墙列的内侧顶角同时用强化混凝土封闭。该规则使曲线两侧墙顶与平台在同一高低分界换层，不会让第三排台阶单独向低侧偏出一格。
- 坡道与 3×8 弯道过渡重合时，最终阶段会重新应用坡步高侧水平过渡实际拥有的接触网凹槽语义：两侧 subtype1 上台阶和中央空气覆盖坡道及外壳的默认混凝土，但不重放外围混凝土，也不恢复低侧已由坡道专用断面接管的旧凹槽。最终墙列封顶后，其正上方由相邻高层普通断面遗留的低优先级顶板计划会直接删除而不是改写为空气，因此既不会覆盖凹槽台阶，也不会在隧道外生成孤立方块或短条。

## `tunnel_metro_2` 双轨断面

该模板的 11×9 直线样板读取自世界 `Hello World!` 的
`-1213 12 -743` 至 `-1213 4 -733`。标记 Y 对应轨床层；以中心路线为横向 0、标记 Y 为高度 0，断面为：

```text
+7  CCCCCCCCCCC
+6  CCCs...sCCC
+5  CC.k...k.CC
+4  Cv.......vC
+3  Cl.......lC
+2  C.........C
+1  Cv.r...r.vC
 0  CCbBb.bBbCC
-1  CCCCCCCCCCC
```

- `C` 为 `railcraft:reinforced_concrete@8`，`s` 为 `reinforced_concrete_slab@1`，`v` 为朝向净空的垂直强化混凝土台阶。
- `r` 是横向 `-2/+2` 的两条 `railcraft:track_flex_reinforced`；`b/B` 分别是 `stone_slab@0` 与 `double_stone_slab@8`，中心点保持空气。
- `k` 是两条轨道各自的刚性接触网。直线、弯道和坡道使用对应模型；支撑件按 `catenarySupportSpacing` 成对放置，目标断面不可用时在前后两格内寻找两轨都为水平直线的断面。
- `l` 是同时放置在最终两侧墙前的隧道灯，索引周期使用 `lightSpacing`。垂直台阶、墙灯以及直线/支架/坡线接触网统一使用最近中心曲线的解析切线判向，不跟随方格路线的瞬时锯齿方向；弯轨和斜线接触网仍按实际前后连接选择模型象限。
- 两条轨道由中心解析断面左右偏移 2 格后分别方格化；内弯重复格会去重，外弯局部缺口只在不经过中心线或另一条轨道的范围内正交补齐。两轨各自按实际连接拓扑选择直轨、弯轨、坡轨和接触网模型。
- 斜线过渡另读取自同一世界的 `-1170 3 -793` 至 `-1181 11 -798`。中心方格路线横移一格时，横移侧轨道提前一行转弯，另一条轨道延后一行转弯；若被省略的旧轨格还会在解析切线换轴处复用，则保留原轨格，避免连续曲线断轨。中心路线经过的两个正交转角格始终为空气，不允许 `44:0` 或轨道进入。
- 斜线的两条内侧 `44:0` 锯齿带只保留对角相接的弱连接；两条外侧带只补远离中心线的正交肘点，保持共享边四向连续。外肘在道床占位合并后生成，轨道格会先从外带候选中剔除，避免把轨道误判成已经存在的连接肘点。
- 双轨候选在解析切线主轴由 X 切换到 Z（或反向）时，还会检查其到整条中心方格路线的距离。会直接贴住其他中心路线格的候选被省略，局部正交连接也禁止进入该一格缓冲；这给两条内侧 `44:0` 留出弱连接空间。生成器分别验证两条外带的共享边连通和两条内带的八向连通，任一侧被切断都会中止预览并返回明确错误。
- 最终道床占位向四个水平方向扩张一格作为混凝土地板和净空缓冲，再从该边界提取侧墙与对角背衬。因此斜线外侧墙会随额外 `44:0` 向外推移，不会占用轨道或紧贴侧道床；上述六行样板的道床和墙面轮廓已作为几何回归基准。
- 九格净空、双轨床和坡道解析断面先合并，再从最终正交边界统一重建底板、侧墙、接触网凹槽及顶板。对角背衬只在两个正交墙格都存在时生成，因此弯道内部不会残留逐断面重叠产生的横墙或立柱。
- 模板使用与 `tunnel_metro_1` 相同的最小半径 12 格和最大坡度 10%。坡步按解析切线法线同时划分两轨及九格断面；只有真实坡轨低端断面的凹槽、顶板及两侧墙上半段抬高一格，前后相邻的普通高低断面保持各自标准高度，坡轨不会同时承担水平弯轨。
- 接近 45° 的曲线坡步会在坡步中心的高、低两层把已属于最终净空的中心格及四个正交邻格并入接触网凹槽；解析法线与正交曲线方格之间因此不会漏下单格强化混凝土，外侧顶板和道床上方台阶不受影响。
- 坡步处的 `43:8` 道床以解析法线两侧各三格为权威断面；栅格轨道的相邻普通高层格继续使用 `44:0`，不会仅因低层轨道处于坡步而升级为 `43:8`。接近对角线时只补齐尚未被 `44:0` 或中心净空占用的断面端角。
- 世界存档只用于开发时读取上述样板；运行与发布时模板不访问 `run/saves/Hello World!`。

## 模板扩展架构

自动生成已拆分为模板无关的执行层与模板自己的结构规划层：

- `AutogenTemplate` 是隧道和桥梁共用的公开扩展接口。每个模板拥有自己的 ID、显示名、类型、参数说明、Tab 补全和 `build` 实现。
- `AutogenTemplateRegistry` 保存运行时模板。内置模板在 `registerBuiltIns()` 中注册；新增本模组模板只需在这里追加一次注册，外部扩展也可在 pre-init 阶段调用公开的 `register(...)`。
- `RoutePlanner` 根据两个方向标记生成五次曲线、连续方格路线、解析切线、断面朝向与轨道兼容坡步。隧道和铁路桥梁都可复用，无需依赖现有地铁隧道砌筑代码。
- `AutogenPlan` 是模板无关的生成结果，统一承载方块操作、路线、预览线框和统计。命令、分 tick 执行、多人预览和撤销只消费该类型。
- `MetroTunnel1Template` 与 `MetroTunnel1Builder` 完整封装原 `tunnel_metro_1` 的参数、材料、尺寸和七格断面特化逻辑。新模板不应向该构建器增加模板 ID 分支。
- `MetroTunnel2Template` 与 `MetroTunnel2Builder` 独立封装 11×9 双轨断面、双轨方格路径与外壳重建，不修改 `tunnel_metro_1` 的生成分支。
- `TunnelTemplateProfile` 只描述地铁模板使用的断面尺寸、曲率/坡度限制和材料。尺寸不同但仍采用相同七格特化算法时应先确认弯道过渡规则是否适用；全新尺寸应拥有独立 Builder，并复用公共路线与计划 API。

新增模板的最小流程：

1. 实现 `AutogenTemplate`，选择 `TUNNEL` 或 `BRIDGE` 类型并解析模板自己的参数。
2. 在 Builder 中调用 `RoutePlanner.plan(selection, minimumRadius, maximumGrade)`。
3. 沿 `RouteGeometry.route` 和 `sectionFacings` 生成该模板自己的断面、桥面、桥墩或洞门方块操作，并创建预览帧。
4. 返回 `AutogenPlan`；通用命令会自动接管 preview、confirm、cancel、status 和 undo。
5. 将实例注册到 `AutogenTemplateRegistry`。无需修改命令、网络包或任务管理器。

桥梁模板已经具备代码接入点，但本次模块化没有内置具体桥梁样式；桥墩落地、跨距划分和水面/地形避让属于未来桥梁模板自己的 Builder 规则。

## 内置模板与安全

两个内置模板的断面资料分别定义在自己的 Template/Builder 模块，模板无关的运行限制定义在 `AutogenConfig`，不会创建 `tunnel_presets.json`。两个模板的默认最大坡度均为 10%；全局限制为路线长度 2048 格、修改 500,000 个方块、每 tick 处理 4096 个方块，预览有效期为 60 秒。

生成前会向执行指令的玩家持续显示青色隧道外描边和统计，不修改世界。描边由最终方块计划提取，包含弯道扩宽与坡道抬高；可见线较亮，被现有方块遮挡的线较暗，并在 `confirm`、`cancel` 或 60 秒有效期结束时消失。系统按 tick 分批生成并在世界存档中保存最近一次撤销数据，包括 TileEntity NBT。生成途中执行 `cancel` 会自动回滚已修改部分。

## Railcraft 依赖

Railcraft 是必需依赖。隧道代码通过 Forge 注册表查找 Railcraft 方块，不直接引用 Railcraft Java 类。相邻 Railcraft 源码当前注册的强化轨道 ID 是 `railcraft:track_flex_reinforced`。

开发环境通过 `META-INF/nebulaecraft_at.cfg` 同步公开 Railcraft 所需的 `TileEntity.REGISTRY` 和 `NBTTagList.tagList`。这是因为 ForgeGradle 3 的反混淆运行环境会读取 `run/mods` 中外部 Railcraft JAR 的 Access Transformer，却不会把这些 SRG 字段规则提前应用到开发用 Minecraft 依赖；发布 JAR 清单也包含同一 `FMLAT` 声明。

本项目使用的 ForgeGradle 3 旧版 1.12 管线在 AT 内容变化后可能把仍含 `Side.BUKKIT` 的 joined 中间包放进 `runClient`/`runServer` 类路径，导致 `NetworkRegistry.newChannel` 在模组构造阶段空指针。`prepareDevForgeJar` 会从正确的 `-recomp.jar` 生成项目本地副本，使用 `gradle/nebulaecraft_dev_at.cfg` 中的 MCP 字段名应用同样两条规则；两个运行任务会自动使用该副本。此逻辑仅服务开发运行，不进入发布模组。

---

## 开发记录

以下内容保留自动隧道工具从初版到当前实现的设计、调试和验证历史；若历史描述与前文冲突，以前文的当前功能说明为准。

### 1. 原始需求

参考 `docs/2026-07-26 15-04-04.mp4`，新增类似 WorldEdit 的隧道自动生成工具：玩家使用工具依次选择两个带方向的标记方块，再通过指令预览和确认生成一条圆滑、曲率缓和的地铁隧道。

确定的注册 ID：

- 标记方块：`nebulaecraft:autogen_marker`
- 选择工具：`nebulaecraft:autogen_wand`

允许使用项目现有 `RegistryHandler` 注册上述方块和物品。

### 2. 路线、交互与指令

- 标记方块使用可旋转箭头表达端点方向；空手右键旋转。
- 标记方块的选择与碰撞范围取自实际模型原始范围 `X 7..9、Y 0..13.5、Z 1.5..9`，并按四个 `FACING` 分别旋转。
- 魔杖依次右键记录起点与终点，选区按玩家保存；两个箭头必须相向。
- 标记坐标代表中央道床位置，生成时被道床替换，撤销时恢复。
- 路线使用五次 Hermite 曲线，端点保持零曲率及零坡度变化。
- 默认最小半径 12 格、最大坡度 10%。
- 默认最长路线 2048 格、最多修改 500,000 个方块、每 tick 修改 4096 个方块。
- 预览有效期 60 秒；全服同一时间只运行一个生成或撤销任务。
- 最近一次生成保存原方块状态和 TileEntity NBT，可在取消、服务器中断或执行撤销时分批恢复。

指令：

```text
/nebulaetunnel preview <preset> <platformColor> <power> [mirror]
/nebulaetunnel confirm
/nebulaetunnel cancel
/nebulaetunnel status
/nebulaetunnel undo
/nebulaetunnel clear
```

别名为 `/ntunnel`，权限等级为 2。供电方式包括：

- `none`
- `catenary`
- `thirdrail_white`
- `thirdrail_yellow`

### 3. 材料约定

- 整块衬砌：Railcraft `railcraft:reinforced_concrete` meta 8（light gray）。
- 水平台阶：`nebulaecraft:reinforced_concrete_slab` subtype1。
- 垂直台阶：`nebulaecraft:reinforced_concrete_vertical_slab`。
- 中央道床：旧 ID `43:8`，即 `minecraft:double_stone_slab` meta 8。
- 两侧道床：旧 ID `44:0`，即 `minecraft:stone_slab` meta 0。
- 强化轨道：`railcraft:track_flex_reinforced`；方块解析仍兼容旧 ID `railcraft:reinforced_track` 和 `railcraft:track_reinforced`。
- 金属平台：`railcraft:post_metal_platform`，颜色由指令决定。
- 接触网模式：使用 NebulaeCraft 钢性接触网系列，而非柔性接触线系列。
- 默认平台位于线路左侧、第三轨位于右侧；`mirror` 同时交换两者。

### 4. 最终直线断面

标记所在中央道床为 Y=0。断面固定为 7×7，从上到下为：

```text
强化混凝土 × 7
强化混凝土 × 2 | subtype1上台阶 | 钢性接触网 | subtype1上台阶 | 强化混凝土 × 2
强化混凝土 | 空气 × 5 | 强化混凝土
垂直强化混凝土台阶 | 空气 × 5 | 垂直强化混凝土台阶
垂直强化混凝土台阶 | 空气 × 5 | 垂直强化混凝土台阶
强化混凝土 | 空气 × 2 | 强化轨道 | 空气 × 2 | 强化混凝土
强化混凝土 × 2 | 44:0 | 43:8 | 44:0 | 强化混凝土 × 2
```

平台、第三轨和灯具是设施覆盖层，会占用相应净空格，但不再替换结构衬砌。金属平台放在完整左侧基础方块上方。

### 5. 旋转方块方向修正

调试时确认 NebulaeCraft 多个带旋转功能方块的子 ID 与原版水平 metadata 规则不同，不能直接假定 `EnumFacing` 的原版序号。

生成器改为显式使用本模组的模型状态映射：

```text
NORTH = 0
SOUTH = 1
WEST  = 2
EAST  = 3
```

并按各模型的实际语义分别处理：

- 垂直台阶和隧道灯：状态表示模型朝向净空，实体贴在相反侧。
- 第三轨直线：`facing` 表示导电轨位于当前方块的哪一侧，不表示线路前进方向。
- 第三轨坡道：结合导电轨所在侧和真实上升方向选择 `slope_1` / `slope_2`。
- 第三轨弯道：根据第三轨处于曲线内侧还是外侧选择 `diagonal_2` / `diagonal_1`，再按实际拐角设置方向。
- 第三轨坡道只替换实际坡轨所在的低端切面，高端相邻平轨恢复普通第三轨。
- 第三轨在有效长度索引 `0, 3, 6...` 使用对应颜色的支架模型；直线使用 `*_support`，弯道使用 `*_support_diagonal`，坡道因没有专用支架模型而保留坡道。
- 单格横移时，第三轨斜线对固定依次使用 `diagonal_1、diagonal_2`。`diagonal_1` 锚定在首个轨道转角的前一索引；`diagonal_2` 使用首个转角索引的模型状态，实际位置同时沿入口反方向和横移方向各移动一格，从而落在 `diagonal_1` 的横向相邻空气格，并位于第一格出段直线第三轨的纵向相邻格。`diagonal_1` 使用首个转角方向的反向，`diagonal_2` 使用第二个转角的原始方向，不能共用同一套 180° 修正。斜线支架仅能替换 `diagonal_1`。
- 支架有效长度计数会把相邻的一组 `diagonal_1 + diagonal_2` 合并为一格；连续 45° 斜线路段拆成首尾相接的单格横移单元，每个单元使用与已验证单格偏移相同的位置和朝向，不再让中间单元退回普通转角逻辑，也不生成额外直线格。
- 钢性接触网坡线：按真实上升方向设置。
- 钢性接触网斜线：按路径前后连接形成的拐角设置。
- Railcraft 强化轨道继续使用其 `EnumRailDirection` metadata。
- Railcraft 弹性轨道的 `onBlockAdded` 会在首次写入时根据当时已存在的邻轨重新计算形状。分批顺序生成时，弯轨的后一格尚不存在，显式设置的转角可能被改回直轨；生成计划现于全部结构操作之后追加轨道定向复写，复写不重复采集撤销数据。

### 6. 隧道灯规则

最初实现为每 8 格在左右墙轮流设置一盏灯。后续修正为：

- 所有灯具均集中在金属平台一侧。
- `lightSpacing` 直接作为路线索引周期；默认值 9 对应索引 `0, 9, 18...`，两盏灯之间保留 8 格。
- 使用 `mirror` 时，平台和全部灯具同步换边。

### 7. 弯道完整断面过渡

方格化平滑曲线的一格横向换位会形成两个相邻轨道拐角。直接叠加两个普通 7 格断面会导致道床互相覆盖、内部残留墙柱以及洞体收窄。

金属平台不跟随两个相邻弯轨硬连接成连续面。朝平台侧换位时，删除旧侧末端两格并在新侧提前补一格，使两段平台只保持对角相邻；背离平台侧换位时执行纵向镜像规则，`mirror` 后同样成立。

最终实现为旋转适配四个方向的 3×8 完整断面过渡。最底层沿换位方向的三行依次为：

```text
CC | 44 | 43 | 44 | 44 | CC
CC | 44 | 43 | 43 | 44 | CC
CC | 44 | 44 | 43 | 44 | CC
```

其中 `CC` 表示两格强化混凝土。其余各层按同一规律同步调整：

- 顶板三行均扩宽为 8 格强化混凝土。
- 普通净空层变为两侧外墙包围 6 格净空。
- 明确清除两个旧断面重叠后位于内部的墙块。
- 两层垂直台阶移动至过渡区的新外缘。
- 接触网槽按 `1格 → 2格 → 1格` 横向换位。
- subtype1 上台阶按与 `44:0` 相同的规律补齐。
- 两个中央道床上分别保留 Railcraft 强化弯轨。
- 平台、第三轨、接触网和灯具优先级高于结构清空操作，不会被过渡模板误删。
- 接触网支撑间隔命中斜线时不再覆盖斜线模型，而是在前后两格内寻找最近的水平直线格；等距时固定优先前一格，四个候选均不符合时忽略本次支撑放置。
- 隧道灯命中扩宽过渡的内缩半区时向平台侧外移一格，贴合实际外墙。
- 初版曾按每个三行过渡的固定坐标补完整 7 格竖列；长弯道中这些局部接缝彼此重叠，竖列会落入净空或在墙外形成凸块。后续仅合并重叠单元仍无法处理 90° 中段换轴，内外侧视频显示墙体继续断连且方向转换处出现侵入净空的整列混凝土。
- 墙体先合并整条路线所有普通断面和专用过渡断面的水平净空，统一清除净空内低优先级结构，再从净空的正交邻域生成完整外边界。曾尝试把所有仅对角接触净空的格都补成完整强化混凝土列，实际会在曲线内侧的每次横移处形成连续立柱并挤占视觉净空。进一步排查发现，正交边界中同时邻接两个净空格的内侧接缝也曾被当作混凝土角柱，从而替换正常垂直台阶；现在这类墙格优先保留普通断面或 3×8 模板已有的台阶朝向，不再生成整列混凝土。
- 90° 弯道的断面轴切换后，格化过渡的 `shift` 会由指向弯道内侧变为指向外侧；继续固定使用 `projectedOldCenter - 3×shift + incoming` 会把后半段背衬镜像到隧道内。现在通过过渡前后各 4–12 格的平衡窗口计算平滑路线曲率：`shift` 指向内侧时使用外侧出段接缝，`shift` 指向外侧时改用外侧入段接缝。多尺度曲率冲突或全为零时不生成可选背衬。候选格还会对最终合并净空做校验，并以低于所有正常断面结构的优先级写入。
- 后续大半径 90° 弯道实测发现，上述按真实曲率只保留物理外圈背衬的做法会令另一侧的内圈对角接缝完全缺失。现改为只恢复每个 3×8 过渡模板的两个精确接缝候选：入段 `before + 4×shift - incoming` 与出段 `projectedOldCenter - 3×shift + incoming`。两者都必须通过最终合并净空及其正交邻域校验，并继续以最低结构优先级写入；这会同时封闭内外圈，又不会恢复“全部对角邻格补整列”所造成的净空立柱。
- 90° 弯道中部会发生断面主轴由 X 到 Z（或反向）的切换。相邻索引若同时被识别为两套互相垂直的一格横移，只采用先到的一套模板，避免同一轨道转角被重复扩宽；统一外轮廓负责封闭两侧及中段，路线首尾仍保持开放。
- 仅去重结构模板会连同后到的新轴墙面朝向一起丢弃；统一外轮廓又会无条件保留先到模板已有的垂直台阶，使换轴中心整段偏向旧轴。现在去重时额外记录旧轴/新轴模板对，但仍只生成一套结构；墙面重建阶段沿最终合并净空寻找每行两侧的真实边界，入段采用旧模板的 `before、oldCorner` 两行，出段采用新模板的 `oldCorner、projectedOldCenter` 两行，仅覆盖垂直台阶朝向。这样南北向与东西向各占两行，不依赖 `EnumFacing.HORIZONTALS` 的等距遍历顺序，也不会重新引入两套 3×8 模板互相切割。边界搜索最多前进两个模板半宽之和 `3 + 4 = 7` 格；若该范围仍处于净空就放弃覆盖，避免命中远处无关轮廓。
- 道床改为按轨道路网重建，而非依赖断面主轴：路线格统一恢复 `43:8`，每个格子的前后连接轴分别贡献左右 `44:0` 邻格；坡轨贡献 `43:8`。转角会自然取得两组侧邻格，连续换位与 X/Z 主轴切换处均不会留下断口。全局混凝土地板完成后再次按三行模板恢复过渡区额外的 `44:0`，保留原 3×8 底层连续图案。
- 实机复核发现，上述“四条截面射线”虽然写入结果在统计上是旧轴 4 格、新轴 4 格，却只命中了两条阶梯墙的远端；中间可见墙格仍继承旧模板，视觉上依然集中为一个方向。现在改为按线路逻辑左右侧分别配对旧、新墙锚，对最终外轮廓执行局部 8 邻接寻路，并覆盖路径上的全部垂直台阶。分配器只使用该墙格实际邻接净空的法线，在每侧将旧、新朝向分成相等数量，奇数格最多相差一格，从几何上避免台阶半砖朝向无净空方向而贴近 `44:0` 边缘。
- 同次实机检查还发现，真正与 `44:0` 共用 XZ 的是金属平台而非垂直台阶。根因是结构换轴会去重第二套模板，同时也漏掉了它的平台调整；相邻普通斜线单元的锚点之间也可能残留旧平台。现在结构模板仍去重，但全部设施过渡都保留：先清除最终 `43:8/44:0` 上方的平台，再检查相邻平台锚点是否已经通过现有平台 8 邻接连通，仅对断开的分量补斜线桥；随后将有效平台格及补桥的对角内肘并入净空、重建墙和完整混凝土基层。连通检查避免在轴切后的正常平台侧额外生成平行格。
- 道床进一步提前为独立的 `TrackbedLayout`：路网邻格与 3×8 模板附加格在墙面生成前合并，统一作为平台禁占区及净空不变量；不再在墙面完成后才补写过渡道床。
- 半径 100 的 E→N 实测路线暴露出换轴去重仍有方向不对称：相邻的旧轴、新轴候选中，新轴候选虽然继续参与平台修正，却没有进入 `TrackbedLayout`、普通净空和接触网凹槽。结果新轴方向应为 `44:0` 和混凝土缓冲的两列被旧轴垂直台阶墙占据。现在完整七层模板仍只使用去重后的候选，但道床和接触网凹槽按全部候选执行带优先级的语义合并；其中接触网行保持“中央空气高于两侧 subtype1、两侧 subtype1 高于外围混凝土”的覆盖关系，不会覆盖优先级更高的实际接触网。
- 最终 `TrackbedLayout` 的每个 `43:8/44:0` 格向四个水平方向扩张一格后再参与净空与墙边界提取，路线开放端之外除外。这显式保证道床与任何非端部墙格都至少隔着一格完整混凝土地板。半径 100 路线中，原先侵入的 `(-1409,6,-266)` 恢复为 `44:0`，`(-1408,6,-266)` 成为混凝土地板及上方净空，墙退至 `x=-1407`。
- 同一路线有 7 盏灯因旧逻辑只检查命中的第一个过渡、且最多外移一格而浮空，其中两盏实际需要外移两格。灯具改为在全部结构和设施确定最终净空后放置：从轨道格沿平台侧扫描到净空边界，选择墙前最后一个净空格，因此不再依赖某个局部过渡模板或固定外移量。
- 新轴道床并入后，部分旧平台格会因正确识别为压占道床而被删除；原补桥只有一种向终点取整的斜线相位，恰好再踩中一格道床时会放弃整座桥。现在只在路线顺序相邻的过渡锚点之间执行有界 8 邻接搜索：搜索限制在局部包围盒和既有净空，桥格距线路 2–3 格、不得属于 `TrackbedLayout`、不得越过开放端或覆盖高优先级设施。半径 100 路线的两个断口各补两格后，平台由 `77 / 3 / 57` 三个分量恢复为 141 格单一分量；旧 128.8 路线及两条路线的反向、mirror 组合也使用同一规则通过。
- 被去重的新轴候选同时恢复精确对角接缝背衬，但候选必须位于最终净空及正交邻域之外且至少对角邻接一个净空格。该条件既补齐真实换轴接缝，又排除墙外孤立列；半径 100、旧 128.8、反向与 mirror 的静态几何回归均没有道床侵入。
- 半径 100 的最新存档逐格读取显示，正向路线的 `44:0` 共 394 格，按共享边四向连通会分成 `112 / 112 / 73 / 69 / 8 / 8 / 6 / 6` 八段；它们在六个位置仅以对角相接，另一正交肘点均为 `43:8`。道床构建现在先标记四向分量，再只补“两个肘点中恰有一个属于中央道床”的唯一外肘，并用并查集避免重复连接。正向补 6 格、反向补 5 格（已有 1 格）后都得到外侧 202 格、内侧 198 格两条完整侧带，正确保留相对 200 格轨道的 `+2 / -2` 弧长差，总计 400 格。
- 道床外肘在墙面净空计算之前加入，因此缺失 `44:0` 原先对应的混凝土地板、上方空气及外移墙体会一起恢复。接缝背衬也改为从最终墙边界反推：只有同时位于两面互相垂直的真实墙之后的对角格才补列，覆盖换轴重叠新增的外缝而不恢复内侧立柱。
- 垂直台阶不再沿用重叠断面先写入且“恰好朝向任一净空”的旧状态。普通歧义墙格按最近路线断面轴选择垂直法线；换轴局部路径继续优先使用原有的两朝向均分器，修复少数台阶沿用错误旧轴的问题。
- Y=30 的新生成存档中，`44:0` 已正确形成外侧 202 格、内侧 198 格，但 289 个墙面垂直台阶仍有 11 个的 metadata 轴与最近线路断面轴相同，且全部集中于换轴局部寻路的两端。根因是均分器为了满足旧轴/新轴数量，允许另一轴候选覆盖到已经进入普通墙臂的边缘格。现在先计算每格垂直于最近断面轴的局部法线，均分候选只有轴与局部法线一致时才能覆盖；换轴中心继续交接两种朝向，边缘则不会再旋转到另一轴。
- 后续读取 `Hello World!` 最高隧道的区块数据：中心轨道位于 Y=39，共 200 格；垂直台阶位于 Y=40/41，每层 289 格。旧 `sectionFacings` 使用半径 6 的方格路线差分，并要求另一轴达到当前轴的 1.5 倍才换轴，因此该路线直到索引 131 才切换断面轴；五次 Hermite 中心曲线的解析切线在索引 102 已满足 `|dz/dt| > |dx/dt|`，两层合计有 54 个台阶 metadata 仍落在错误轴。现在方格化时同时保存每个中心格对应的曲线参数，以解析导数直接选择断面轴，并在最终结构完成后逐格统一校正全部垂直台阶：切线更接近 X 轴时使用 `north/south`，更接近 North/South 轴时使用 `east/west`，不再使用 1.5 倍迟滞或换轴数量均分决定最终朝向。
- 最高隧道 Y=42 的 23 盏隧道灯中也有 4 盏沿用了上述迟滞旧轴；Y=43 的普通/支撑刚性接触网在该路线中未出现轴错误，但生成接口仍传入方格路线的瞬时前进方向。现将解析切线提升为全部旋转设施的共同方向源：隧道灯、直线/坡线第三轨取切线法线，直线/支撑/坡线刚性接触网取切线轴，坡线再叠加真实上升方向。Railcraft 轨道和两类斜线设施继续按实际相邻格连接选择曲线 metadata 或斜线象限，否则会破坏锯齿路线的物理连通；其周围断面和相邻直线设施仍使用解析切线。
- 使用解析切线重新生成的最高隧道中心道床位于 Y=46。逐格比对最终地板、两层墙面、Y=51 接触网凹槽层和 Y=52 顶板后确认，中段外侧恰有 4 个应属于统一外壳但为空气的体素：顶板 `(-1422,52,-250)`、`(-1422,52,-249)`、`(-1421,52,-250)`，以及凹槽外层 `(-1422,51,-249)`。病因不是单个接缝候选算错，而是结构语义不对称：X/Z 换轴去重第二套重叠 3×8 实体模板后，道床及其扩张净空仍合入最终布局，地板和墙也从该净空重建，但凹槽默认混凝土与顶板只由保留模板写入。现将墙面重建提升为统一外壳重建，按最终合并净空同时生成地板、墙、凹槽默认层及顶板；显式空气、水平台阶、接触网和坡道抬高结构继续以更高优先级覆盖。该修改修复产生漏洞的层级分裂，而非按坐标追加补块。
- 再次读取 `Hello World!` 最新、最高隧道后确认：Y=63 的中心强化轨道共 200 格，白色第三轨却只有 144 格。切线主轴切换前的每个 `diagonal_2` 候选都落在中心轨道坐标，因第三轨优先级低于轨道而未能写入；剩余 `diagonal_1` 的 metadata 也使用了只适用于另一侧单格横移的 180° 修正。根因是旧重锚公式只校准过“横移方向等于第三轨所在侧”的情形，却被无条件复用于相反侧。
- 第三轨现改为独立的有序侧线路径。横移位于第三轨侧时保留已验证的前置 `diagonal_1 → diagonal_2` 锚定；位于相反侧时改为后置、反序的 `diagonal_2 → diagonal_1` 锚定。解析切线换轴处先排除压中心轨道和重复的候选，外侧缺口则以不经过中心轨道的最短四向路径补齐，最后从侧线的真实相邻连接统一推导型号和方向。对存档的同一条 200 格路线静态复算，内侧为连续 198 格、外侧 mirror 为连续 202 格，均无中心轨道重叠；白色和黄色第三轨共用该布局逻辑。

### 8. 坡道专用断面

强化轨道位于一格高差的下端并使用上坡轨道状态时，不再沿用普通 7 层断面，而使用 8 层专用断面：

- 顶板相对道床抬高至第 8 层。
- 接触网凹槽的 subtype1 台阶位于坡线接触网上方一层。
- 左右垂直强化混凝土台阶由两层增为三层。
- 坡道轨道下方三格道床全部使用 `43:8`。
- 上端平轨格保持普通断面，使其顶板高度与坡道格抬高后的顶板一致。
- 隧道灯命中坡道轨道格时向上移动一格。
- 接触网支撑间隔命中坡线时，采用与斜线相同的前后两格避让规则；没有水平直线候选则跳过本次支撑。
- 坡线接触网仅放置在实际使用上坡轨道状态的低端切面；相邻高端平轨切面恢复直线接触网。
- `lightSpacing` 与 `catenarySupportSpacing` 直接作为索引周期使用，不在运行时执行 `+1`；两者默认值均为 9，目标索引为 `0, 9, 18...`。
- 统一弯道墙面最初按包含 Y 的 `clearFootprint` 在同层提取边界；一格高差处，低层断面会把相邻高层净空误判成外部并向上写出横断墙。现在边界候选同时检查同 XZ 的 `y-1 / y / y+1` 三层净空，并在写入每个墙体 voxel 前再次避让其下方 `clearHeight` 范围内的净空底面。该处理只合并格化路线允许的一格坡步，不会把相隔多层的结构投影成同一 XZ 轮廓。
- 墙面重建前会快照原断面的垂直台阶。若某列在 `y=clearHeight` 已有坡道第三排台阶，重建后仍使用该第三排，并按最终墙—净空法线统一方向；普通断面该位置是整块混凝土，因此不会被误扩成三排台阶。
- 读取 `Hello World!` 新增样板后，确认低层生成隧道把高差直接放在曲线横移边 `(-1525,-139) → (-1524,-139)` 上，令坡轨同时承担水平弯轨，并使高低层分界沿方格横移方向切开 3×8 模板。上层手调样板把低端坡轨放在 `(-1524,14,-141)`，其解析切线约为 `(24.2637,-62.8676)`；七格法线断面取整后为 `(-1527,-142) … (-1521,-140)`，两端三格高垂直台阶坐标与存档完全一致。
- 路线现在先只方格化 X/Z，再单独放置高程台阶。曲线横移模板的入段、横移、出段三条边全部标记为不可放坡；取整高差若命中模板，便在低侧选择最近的解析切线同轴边。正向下降选择索引 41、反向上升选择反向索引 37，二者对应同一个物理低端 `(-1524,-141)`，恢复路线正反向对称。
- 新增坡度断面布局：每个候选地板、道床侧带、平台和第三轨都会在最近线路格附近比较其到各解析切线法线的纵向投影，并归属投影绝对值最小的断面；归属于低端坡轨的格子即为坡道断面。统一外壳据此同步抬升净空、接触网凹槽和顶板，并在最终结构阶段沿真实法线重放完整八层坡道断面。垂直台阶第三排不再依赖旧卡迪纳尔模板是否恰好在最终墙坐标留下台阶。
- 最新生成隧道中，灯具 `(-1603,10,-166)` 的南邻仍是空气，实际墙面位于 `z=-164`；同批其余 10 盏均直接邻墙。该灯从高层中心轨道向平台侧扫描时，旧逻辑始终携带中心轨道 Y，因下一横向格已归属低层断面而误判为非净空，提前停了一格。灯具扫描现与结构共用 `GradeLayout`：每次横移都重新取得切线断面楼层，并按最终灯位所属断面决定是否使用坡道抬高层，因此会继续到真实墙前。
- 2026-08-01 读取 `Hello World!` 最近一次生成数据：隧道路线从 `(-1495,11,-422)` 至 `(-1397,7,-323)`，198 个轨道格的四处低端坡轨分别在索引 `45、96、149、156`，相邻间距为 `51、53、7`。理论 Hermite 高程阈值约为 `46、82、116、153`；连续方格弯道使中段大量三边过渡互相重叠，旧贪心算法将第三个坡步大幅推后后，仍让第四个坡步就近落下。现使用线性复杂度的动态规划同时选择所有坡步，并以 `ceil(1 / maximumGrade)` 作为硬性最小间距。对该路线的静态复算结果是物理索引 `45、96、149、165`，间距 `51、53、16`。
- 玩家所在的第二处坡道低端为 `(-1425,9,-396)`，解析法线约为 `(0.7001,-0.7140)`。旧的单位法线取整使横向序号 `-2/-1` 同时落在 `(-1426,-395)`，`1/2` 同时落在 `(-1424,-397)`，七格断面实际只剩五格，两端三层墙柱被写进最终净空。法线现以切线 X/Z 分量的较大绝对值归一化，该断面将生成七个唯一格 `(-1428,-393) … (-1422,-399)`，墙柱回到净空外缘。
- 用修复后代码重新生成同一 X/Z 路线（整体上移 9 格）后，读取到四处坡轨索引为 `33、45、149、160`，间距 `12、104、11`。运行配置中 `maximumGrade` 已改为 10%，因此它们满足 10 格最小间距，但中段的连续三边模板仍使可用坡边呈两端聚集。进一步检查确认理论阈值附近的 `80、114、152` 都是“横移已结束、低端轨格为直轨”的出段边，旧规则仅因它们属于三边模板便整组禁用。现仅禁止横移边、高侧外边及低端弯轨，允许模板在低侧外边跨层衔接坡道；静态复算为 `45、80、114、152`，间距 `35、34、38`。
- 2026-08-02 分析标记 `(-1254,11,-550)` 向西至 `(-1354,7,-450)` 向北的 preview 失败：201 格路线的坡步索引为 `46、81、119、155`，其中低侧过渡锚点仍使用 `after/before` 中心格的原始 Y。默认侧锚点 `(-1332,9,-512)` 的地板 Y=8，但 `GradeLayout` 将该 X/Z 归属于 Y=9 断面；`mirror` 侧的 `(-1316,10,-534)` 同样应从 Y=10 抬到 Y=11。两者都没有压道床，却因不属于错误高度的 `clearFootprint` 被补桥入口拒绝。现将平台过渡的删除与新增锚点全部经 `GradeLayout.floorAt(...).up()` 归一化，避免把真实的高度错位误报为道床侵占。
- 随后读取同日最新高层隧道：从北侧数第二处坡道的低端道床位于 `(-1300,23,-541)`，解析法线内侧应为 `43:8` 的 `(-1299,23,-540)` 实际为空气，其正上方 `(-1299,24,-540)` 反而残留 `44:0`。原因是道床和净空先按最近普通断面归层，坡道专用断面随后只重放空气与顶板，没有把解析法线的三格道床纳入 `TrackbedLayout`；最终道床重建便在坡道清空之后再次写回了错误高层侧道床。现先把五格坡道净空和三格 `43:8` 合入最终布局，并移除同 XZ 高一层的道床/净空占位，侧带补肘也禁止重新跨层写到中央道床上方。
- 同一路线最接近曲线中段的坡道低端为 `(-1330,20,-518)`、`(-1322,21,-526)`。解析法线 `±3` 端点在接近 45° 时落到两面正交墙后的对角背衬，不是真实墙面；旧坡道重放仍把四列中段强制改成垂直台阶，并在 `(-1327,27,-515)`、`(-1325,28,-529)` 留下与周围顶板四向不相连的强化混凝土单格。现由最终净空提取的真实墙边界独占墙面材料，坡道重放只处理 `-2..2` 的净空与凹槽；对角背衬以更高结构优先级恢复整块混凝土，并且只有四向邻接非背衬的最终抬高顶板时才随之抬高，因此不会再留下孤立顶板。
- 继续读取 2026-08-02 最新生成的同形隧道后，玩家用红羊毛标出应为 subtype1 上台阶的 `(-1192,44,-368)`、`(-1192,44,-367)`、`(-1175,43,-388)`、`(-1174,43,-388)`，并用黑羊毛标出应完全不参与生成的 `(-1214,47,-353)`、`(-1214,47,-352)`。这些坐标证明先前归因于外角背衬并不准确：红色位置属于坡步高侧的水平弯道凹槽，随后被坡道重放的高优先级混凝土覆盖；黑色位置则是最终墙列已经在低层封顶后，较高普通断面遗留的低优先级顶板，其中一格还令相邻背衬误判为需要抬高。现单独收集紧邻低层坡步的高侧水平过渡，不受完整 3×8 模板的解析切线轴筛选影响；全部坡道、墙面和背衬完成后只重放这些过渡的显式台阶/空气语义，并在每个最终墙列封顶后删除紧邻其上的低优先级混凝土计划项。对最新 201 格轨道路网静态复算，高侧候选为索引 `78、116、152`，四个红色坐标全部进入台阶集合，两个黑色坐标均未进入凹槽集合。墙顶删除不写入空气，因此不会破坏生成前该位置已有的世界方块或增加无意义的撤销记录。
- 隧道内凹槽修复生效后，再读取 2026-08-02 10:56 生成的 201 格上层隧道。玩家在四个外部多余混凝土上方放置钻石块：`(-1199,70,-360)`、`(-1198,70,-360)`、`(-1170,73,-400)`、`(-1170,73,-399)`，对应的多余混凝土位于 Y 低一格。两组位置分别紧邻坡步 `(-1201,61,-363) -> (-1200,62,-363)` 与 `(-1167,64,-397) -> (-1167,65,-398)`，每组都是一根高侧最终墙列及其外角背衬。墙列按 X/Z 解析断面判断坡道时投影到了低端坡轨，却没有校验墙脚已位于高一层，因而误用抬高一层的坡道墙顶；背衬随后检测到这格墙顶又被连带抬高。现在坡道墙顶判断同时要求最近解析断面是坡轨、且该断面楼层等于墙脚 Y；这两根高侧墙恢复普通高度后，背衬不再满足抬高条件，四个钻石块下方的位置都不进入生成计划。真正位于低层的坡道墙仍保留三排垂直台阶和抬高顶板。
- 读取 2026-08-02 11:05 与 11:07 的最新两条同形隧道：下层标记为 `(-1193,4,-440) -> (-1282,8,-459)`，上层整体抬高 9 格后由玩家修成理想样板。109 个轨道格的四处坡步完全一致；从西侧出发的第一处低端为 `(-1258,7,-457)`。将上层坐标减去 9 后，样板只改动 6 格：金属平台由 `(-1259,9,-455)` 下移至 `(-1259,8,-455)`；`(-1259,9,-454)` 改为第三排垂直台阶；北侧第三排台阶由 `(-1257,11,-460)` 移至 `(-1258,11,-460)`；并在 `(-1257,12,-459)` 补墙顶混凝土。根因是该坡道解析法线的 `-2` 格 `(-1259,-455)` 到低端法线的纵向距离为 `0.4817`，却被相邻高层法线以 `0.4639` 的微小优势抢走；后续净空和道床会强制恢复低层，普通平台和墙顶选择却仍保留了不一致的高层归属。现在五格坡道净空直接注册为 `GradeLayout` 的权威低层断面；仅当发现这种取整冲突时，第三排墙体才在实际墙边界中选择最接近坡步中点的一列，并封住移位后内侧墙顶。静态复算中另外三处坡步均不存在法线归属冲突，因此不会改变其现有结构。
- 预览从一次性中心线粒子改为客户端持久线框。服务端从最终计划的实体方块列中提取每个解析切线断面的左右外墙上下角，客户端连接四条纵向外边并每 8 格补一个断面环；参考 WorldEditCUI 的双深度通道画法，可见线使用高亮青色，遮挡线使用低透明度暗青色。线框仅发送给预览发起者，并随 `confirm`、`cancel` 或有效期到期清除。

### 9. 崩溃排查

首次进入世界时，`crash-2026-07-26_16.41.04-server.txt` 显示 Railcraft 在反序列化进度条件时读取 `TileEntity.REGISTRY`，触发 `IllegalAccessError`。

最终处理：

- 新增 `META-INF/nebulaecraft_at.cfg`。
- 公开 `net.minecraft.tileentity.TileEntity field_190562_f`（`REGISTRY`）。
- 在 ForgeGradle 配置和 JAR manifest 中声明该 Access Transformer。
- 删除 `RegistryHandler` 中引用客户端专用字段的调试输出，避免独立服务端加载失败。

随后进入包含 Railcraft 箱子矿车的世界时，列车存档读取又因访问 `NBTTagList.tagList` 触发 `IllegalAccessError`（`crash-2026-07-26_17.40.18-server.txt`）。因此最终 AT 文件同步 Railcraft 当前实际触发的两条必要规则：

- `TileEntity.REGISTRY`
- `NBTTagList.tagList`

未把 Railcraft AT 中其余尚未触发的字段规则无差别复制进项目。

补充处理 ForgeGradle 3 开发运行缓存问题：增加 `prepareDevForgeJar`，从不含 `Side.BUKKIT` 的 `-recomp.jar` 生成带两条 MCP 名访问规则的本地开发副本，并在 `runClient`、`runServer` 启动前自动替换 joined 中间包。否则新增第二条 AT 后会在进入主菜单前先于世界加载触发 `NetworkRegistry.newChannel` 空指针，这不是隧道或 Railcraft 存档逻辑本身的异常。

### 10. Java 内置模板（迁移前记录）

该阶段曾把隧道模板和运行限制直接定义在 `TunnelConfig` 的 Java 常量对象中，不再创建或读取 `config/nebulaecraft/tunnel_presets.json`，因此也不再提供 `/ntunnel reload`。默认预设 ID 更名为 `tunnel_metro_1`，默认最大坡度调整为 10%。当前架构已由后述模块化改造替代。

### 11. 主要实现文件

- `src/main/java/net/kuina/nebulaecraft/autogen/template/AutogenTemplate.java`
- `src/main/java/net/kuina/nebulaecraft/autogen/template/AutogenTemplateRegistry.java`
- `src/main/java/net/kuina/nebulaecraft/autogen/template/tunnel/MetroTunnel1Template.java`
- `src/main/java/net/kuina/nebulaecraft/autogen/template/tunnel/MetroTunnel1Builder.java`
- `src/main/java/net/kuina/nebulaecraft/autogen/route/RoutePlanner.java`
- `src/main/java/net/kuina/nebulaecraft/autogen/AutogenPlan.java`
- `src/main/java/net/kuina/nebulaecraft/autogen/AutogenConfig.java`
- `src/main/java/net/kuina/nebulaecraft/autogen/CommandNebulaeTunnel.java`
- `src/main/java/net/kuina/nebulaecraft/autogen/AutogenSelection.java`
- `src/main/java/net/kuina/nebulaecraft/block/BlockAutogenMarker.java`
- `src/main/java/net/kuina/nebulaecraft/item/ItemAutogenWand.java`
- `src/main/resources/META-INF/nebulaecraft_at.cfg`
- `gradle/nebulaecraft_dev_at.cfg`

### 12. 验证与测试流程

每次主要修改后均使用 JDK 8 执行编译；最终完整弯道断面修改通过：

```bash
env JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-8.jdk/Contents/Home \
  ./gradlew -Dnet.minecraftforge.gradle.test_certs=false compileJava processResources

env JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-8.jdk/Contents/Home \
  ./gradlew -Dnet.minecraftforge.gradle.test_certs=false jar
```

构建结果：

```text
build/libs/NebulaeCraft-1.12.2-2.25.jar
```

90° 弯道方向转换、内外圈接缝及中段 `2 + 2` 台阶朝向交接修复后再次执行 `compileJava processResources`，构建成功。

第二次世界加载崩溃修复后还验证了：

- `compileJava processResources prepareDevForgeJar` 成功。
- 开发 Forge 副本中仅有 `Side.CLIENT`、`Side.SERVER`，且 `TileEntity.REGISTRY`、`NBTTagList.tagList` 均为 public。
- `runClient` 使用该副本启动，Forge、Railcraft、NebulaeCraft 均完成加载并进入主菜单，不再出现 `NetworkRegistry` 初始化崩溃。

游戏内重复测试前，应先执行：

```text
/ntunnel undo
```

然后重新选择标记、执行 `preview` 并确认生成，避免旧结构影响观察结果。

### 13. 2026-08-05 模板模块化

- 删除同时承担预设和运行限制的 `TunnelConfig`；全局限制迁入 `AutogenConfig`，`tunnel_metro_1` 的资料迁入自己的模板模块。
- 新增 `AutogenTemplateRegistry`，指令根据模板 ID 动态分发参数解析、Tab 补全和结构规划，不再硬编码模板参数位置。
- 新增模板无关的 `AutogenPlan`；既有预览网络包、分 tick 执行与撤销链可直接处理隧道或桥梁计划。
- 将五次曲线、方格化、解析切线和坡步匹配抽取到 `RoutePlanner`，原地铁隧道构建器只保留它独有的断面、弯道壳体、轨道、站台及供电设施逻辑。
- 原 `TunnelBuilder` 移入 `template/tunnel` 并更名为 `MetroTunnel1Builder`，明确它不是所有模板都应修改的中心分发器。

### 14. 2026-08-06 `tunnel_metro_2` 曲线复核

- 从客户端日志确认最新双轨曲线为 `(-1135,18,-1114) → (-1035,18,-1013)`，中心路线共 202 格，解析切线主轴在索引 101 从 X 切换到 Z。
- 旧结果的 `44:0` 八向连通分量为 `208 / 196 / 75 / 72 / 71 / 71`：两条外带完整，但两条内带都在主轴切换处被拆成两段。原因是卡迪纳尔偏移候选贴到另一格中心路线，中心留空阶段随后删除了对应内侧道床。
- 轨道候选与局部连接现保留至少一个非中心格的缓冲；对同一路线静态复算后，`44:0` 只剩四条连续带，分量为 `208 / 196 / 149 / 144`。两条外带保持共享边连续，两条内带保持样板要求的对角弱连接，中心路线仍全部为空气。
- 模板 2 的最终垂直台阶、成对墙灯以及普通/支架/坡线接触网改为复用模板 1 的解析切线判向。轨道与斜线接触网继续使用实际线路拓扑，避免以单一卡迪纳尔切线破坏弯角连接。
- 同一路线整体上移到 Y=28 后再次生成，外轨在索引 98–104 的切线主轴切换处出现 `3×3` 的局部补线缺口。旧的有界最短路只按“离中心线更远”打破等长候选，因而把同样为 6 步的斜向连接方格化成 `EEE + SSS`，连同转角表现为两段各 4 格的直轨凸起；内轨没有这段大跨度缺口，所以不受影响。补线现在先比较候选到两端锚点斜率的垂直误差，按 `X/Z` 交替逼近解析切线，只有普通一格对角过渡误差相同时才沿用远离中心线的早移/晚移规则。静态复算中该段由 `(-1063,-1088) → (-1060,-1085)` 的 L 形改为连续锯齿，整条轨道数量仍为外轨 206、内轨 198；四条 `44:0` 带仍连续，八向分量为 `208 / 196 / 147 / 144`。

### 15. 2026-08-07 `tunnel_metro_2` 坡道外壳修复

- 对齐世界 `Hello World!` 中两条相同路线的双轨坡道：旧生成结果中心为 `x=-1148`，人工修正后的新结果中心为 `x=-1131`，共同范围为 `z=-1275..-1248`。50 个差异方块全部集中在坡步附近 `z=-1262..-1260`。
- 旧逻辑为捕获斜向坡墙而把坡道法线的任意四邻格也判作抬高外壳，导致坡步前后的普通高、低断面各有一排凹槽和顶板被错误抬高。现改为与 `tunnel_metro_1` 相同的断面归属判定：法线明确覆盖的格只接受坡道低层，额外曲线墙格则必须由最近解析断面判定为坡轨低端，抬高不再向纵向相邻断面扩散。
- 坡道断面的下排垂直台阶保持原高度，上排垂直台阶及其顶部整块墙随坡道顶板抬高一格；这与人工修正后的两侧墙完全一致，并保持普通断面的两排墙面高度不变。

### 16. 2026-08-09 `tunnel_metro_2` 曲线坡道凹槽补缝

- 最新样本路线为 `(-1227,75,-1079) → (-1127,79,-980)`，长度 `158.74`、最小半径 `80.56`、最大坡度 `4.24%`。世界中的四个青金石标记位于 `(-1206,82,-1014)`、`(-1204,83,-1014)`、`(-1188,83,-999)`、`(-1188,84,-997)`。
- 四个位置都由最终净空格在 `RECESS_Y` 留下的默认强化混凝土产生，不属于侧墙或对角背衬。对应坡步的解析法线接近 45°，中央凹槽使用对角取整，而相邻普通断面和双轨路径使用正交方格，导致高、低两层各漏掉一个正交连接格。
- 每个坡步现以低端中心 X/Z 为基准，在高、低两层检查中心格和四个正交邻格；只有已经属于最终净空的格才补入凹槽空气语义。该样本新增 8 个语义格，其中 4 个正是标记位置，另外 4 个原本已由跨层净空覆盖；外侧顶板不会被扩大清除，轨道位置的水平凹槽台阶仍以更高优先级覆盖空气。

### 17. 2026-08-09 `tunnel_metro_2` 曲线坡道道床断面修复

- 最新样本沿用 `(-1227,85,-1079) → (-1127,89,-980)` 路线。钻石标记位于 `(-1222,86,-1039)`、`(-1202,87,-1015)`、`(-1189,88,-995)`，表示这里应为 `44:0` 却生成了 `43:8`；金块标记位于 `(-1208,86,-1011)`、`(-1185,87,-1001)`，表示这里应为 `43:8` 却为空气。
- 原侧道床判定只要轨道格属于坡步，就把经 `floorAt` 折算后的两侧格一律升级为 `43:8`。曲线坡道中，低层轨道的一侧可能折算到相邻普通高层断面，因而三个本应保持 `44:0` 的格被误判。现在只有侧格仍与坡轨同层时才继承坡步实体语义，解析坡道断面本身仍保持 `43:8`。
- 两个缺失格来自栅格轨道带与解析法线断面的差异：一个是解析法线的外端，另一个是接近 45° 时法线端点与方格端角之间的一格正交缝隙。双轨栅格带完成并通过连续性验证后，现合并坡步两侧各三格的权威解析道床；对角端角仅在未被既有 `43:8`、`44:0` 或路线中心净空占用时补齐。五个标记坐标的语义均已定点复算，中心路线没有新增实体格。
